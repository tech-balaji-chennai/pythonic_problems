//Reverse A Number - Pseudo Code

BEGIN
    INPUT number
    ASSIGN mod = 0, rev = 0
    WHILE (number > 0) DO
        COMPUTE mod = number % 10
        COMPUTE rev = (rev * 10) + mod
        COMPUTE number = number / 10
    END WHILE
    OUTPUT rev
END
